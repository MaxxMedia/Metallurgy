import { createStaticReactPage } from "@/lib/content/react/create-static-react-page";

const { Page, generateMetadata } = createStaticReactPage("register");

export { generateMetadata };
export default Page;
